const QUOTATION_STATUS = {
  DRAFT: 'DRAFT',
  SUBMITTED: 'SUBMITTED',
  ACCEPTED: 'ACCEPTED',
  REJECTED: 'REJECTED',
  EXPIRED: 'EXPIRED',
  CLOSED: 'CLOSED',
  WITHDRAWN: 'WITHDRAWN'
};

const AVAILABILITY_RESULT = {
  FULLY_AVAILABLE: 'FULLY_AVAILABLE',
  PARTIALLY_AVAILABLE: 'PARTIALLY_AVAILABLE',
  UNAVAILABLE: 'UNAVAILABLE'
};

const CLOSED_REASON = {
  ANOTHER_QUOTATION_ACCEPTED: 'ANOTHER_QUOTATION_ACCEPTED',
  REQUEST_CANCELLED: 'REQUEST_CANCELLED',
  REQUEST_EXPIRED: 'REQUEST_EXPIRED'
};

const DEFAULT_CURRENCY = process.env.DEFAULT_CURRENCY || 'LKR';
const MIN_PREPARATION_MINUTES = 5;
const MAX_PREPARATION_MINUTES = parseInt(process.env.QUOTATION_MAX_PREPARATION_MINUTES, 10) || 1440;

module.exports = {
  QUOTATION_STATUS,
  AVAILABILITY_RESULT,
  CLOSED_REASON,
  DEFAULT_CURRENCY,
  MIN_PREPARATION_MINUTES,
  MAX_PREPARATION_MINUTES
};
